# Core model + inference
torch==2.6.0
transformers==4.46.2
accelerate==0.30.1

# MCP server + FastAPI backend
fastapi==0.110.0
uvicorn==0.29.0

# SSH + HTTP utilities
paramiko==3.4.0
requests==2.31.0
pydantic==2.7.1

# Optional (pretty logging)
rich==13.7.1
